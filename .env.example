# Example configuration for vid2gif-webui
# Copy this file to ".env" and adjust values as needed.

# Base directory where temporary job folders and GIFs are stored.
TMP_BASE_DIR=tmp

# Number of seconds before finished jobs and their temporary files are cleaned up.
JOB_TTL_SECONDS=3600

# Maximum number of concurrent ffmpeg conversions.
FFMPEG_MAX_CONCURRENT=4
